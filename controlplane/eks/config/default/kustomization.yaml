# Adds namespace to all resources.
namespace: capi-eks-control-plane-system

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

resources:
- namespace.yaml

bases:
- ../rbac
#- ../manager
